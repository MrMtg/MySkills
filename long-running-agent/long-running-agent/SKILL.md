---
name: long-running-agent
description: 长时间运行的 Agent 系统框架 - 双 Agent 架构，支持跨会话状态持久化、任务自动化循环开发、规格驱动开发模式
version: 1.1.0
author: Claude & SamuelQZQ
tags: [agent, workflow, automation, long-running, project-management, spec-driven]
---

# Long Running Agent System

一个完整的长时间运行 Agent 系统框架，用于跨多个会话完成复杂项目的开发。

## 核心特性

- **双 Agent 模式**：初始化 Agent（第一会话）+ 编码 Agent（后续会话）
- **状态持久化**：通过 Git + task.json + progress.txt 跨会话恢复状态
- **单一真理来源**：task.json 定义所有工作，只能修改 `passes` 字段
- **强制阻塞机制**：Agent 遇到无法解决的问题必须停止并求助
- **监控模式**：monitor-claude.sh 持续监控并自动驱动 Claude 工作
- **规格驱动开发**：需求澄清与开发实施分离，干净上下文

## 三种使用模式

### 模式一：传统对话模式（需求明确）

```
你："帮我初始化一个新项目"
→ AI 收集需求 → 生成约束文件 → 启动监控 → 自动开发
```

### 模式二：规格驱动模式（复杂项目/需求模糊）

```
./spec-driven-init.sh
→ 输入一句话描述 → AI 系统性提问 → 生成完整规格 → 新会话实施
```

### 模式三：现有项目模式（老项目）

```
./scan-existing-project.sh
→ 扫描项目 → 生成 task.json → 启动监控 → 继续开发
```

## 快速开始

### 规格驱动模式（推荐用于复杂项目）

```bash
# 1. 初始化规格驱动模式
./spec-driven-init.sh

# 2. 输入项目描述（一句话）

# 3. 对 Claude 说："开始规格澄清"
#    Claude 会自动读取 MINIMAL_SPEC.txt 并开始系统性提问

# 4. 回答问题，Claude 生成完整的 SPEC.md

# 5. 新建干净会话，让 Claude 按 SPEC.md 自主开发
```

### 传统模式

```bash
# 对 Agent 说："帮我初始化一个新项目"
# 收集需求 → 生成文件 → 启动监控
```

### 监控模式（所有模式通用）

```bash
# 启动监控
./monitor-claude.sh --start

# 查看状态
./monitor-claude.sh --status

# 停止监控
./monitor-claude.sh --stop

# 解决阻塞后恢复
./monitor-claude.sh --resume
```

## 核心文件

| 文件 | 作用 |
|------|------|
| `CLAUDE.md` | Agent 工作流程约束 |
| `task.json` | 任务列表（单一真理来源） |
| `SPEC.md` | 完整技术规格（规格驱动模式） |
| `MINIMAL_SPEC.txt` | 最小规格（一句话描述） |
| `progress.txt` | 进度日志 |
| `monitor-claude.sh` | 后台监控脚本 |
| `run-claude-loop.sh` | 单次会话运行器 |

## 规格驱动开发流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│ Phase 1: 最小规格                                                          │
│ ./spec-driven-init.sh → 输入一句话描述 → 保存到 MINIMAL_SPEC.txt            │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Phase 2: AI 主动设问                                                       │
│ 用户说："开始规格澄清" → Claude 自动读取 MINIMAL_SPEC.txt → 系统性提问    │
│   • 功能范围（核心功能、MVP、后续版本）                                     │
│   • 边缘案例（空输入、超长、并发、大数据量）                                 │
│   • 错误处理（各种错误的响应方式）                                           │
│   • 数据设计（存储、关系、持久化）                                           │
│   • 接口设计（端点、请求方式、认证）                                         │
│   • 性能要求（响应时间、并发、缓存）                                         │
│   • 安全要求（认证、数据保护、攻击防护）                                     │
│   • 测试要求（测试类型、覆盖率）                                           │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Phase 3: 完整规格                                                          │
│ 生成 SPEC.md 完整技术规格                                                  │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Phase 4: 干净会话实施                                                      │
│ 新会话，清空上下文，只给 SPEC.md，AI 自主开发                              │
└─────────────────────────────────────────────────────────────────────────┘
```

## 监控模式工作流程

```
Claude 正在运行？
    │
    ├─ 是 → 等待 5 秒，继续检测
    │
    └─ 否 → 检查是否有阻塞？
              │
              ├─ 有 → 停止，等待人工解决
              │
              └─ 无 → 自动启动下一个会话
```

## task.json 格式

```json
{
  "project": "项目名称",
  "tasks": [
    {
      "id": 1,
      "title": "任务标题",
      "description": "详细描述",
      "steps": ["步骤1", "步骤2"],
      "passes": false,        // ← 只能改这个字段
      "priority": "critical|high|medium|low",
      "dependencies": []
    }
  ]
}
```

## 核心规则

1. **每会话一个任务** - 专注于完成一个任务
2. **测试后再标记完成** - 所有步骤必须通过
3. **分层测试** - 大幅修改用浏览器测试，小修改用 lint+build
4. **遵守编码规范** - 严格按照 rules.md 执行
5. **在 progress.txt 中记录** - 帮助未来的 Agent 理解工作
6. **一个任务一个 commit** - 代码+progress.txt+task.json 一起提交
7. **永远不要移除任务** - 只能将 `passes: false` 改为 `true`
8. **阻塞时停止** - 需要人工介入时，输出阻塞信息并停止

## 阻塞处理

当 Agent 遇到以下情况必须停止并求助：
- 缺少环境配置（API密钥、数据库等）
- 外部服务不可用
- 测试无法进行
- 需求不明确

阻塞标记格式：
```markdown
🚫 任务阻塞 - 需要人工介入
**当前任务**: [任务ID - 任务标题]
**阻塞原因**: [具体原因]
**需要人工帮助**: 1. [...] 2. [...]
```

## 完整文档

完整文档请参考：
- https://github.com/MrMtg/Auto_agents

## 脚本文件

- `spec-driven-init.sh` - 规格驱动模式初始化
- `scan-existing-project.sh` - 现有项目扫描
- `monitor-claude.sh` - 后台监控脚本
- `run-claude-loop.sh` - 单次会话运行器
- `init.sh` - 环境初始化脚本
